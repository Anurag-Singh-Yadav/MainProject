import React from 'react'

function InterviewCard({name, userPhoto, company,linkedin_id,created_on,seleted,position,round}) {
  return (
    <div>
      
    </div>
  )
}

export default InterviewCard
